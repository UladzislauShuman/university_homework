#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <string>
#include <vector>
#include <sstream>

bool checkDate(const std::string& date);
bool DateCompare(const std::string& dateA,const std::string& dateB);
#endif // FUNCTIONS_H
